server:
  port: 8001
spring:
  application:
    name: item-server
  datasource:
    url: jdbc:mysql://localhost:3306/day04?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka

